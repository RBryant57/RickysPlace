@model IEnumerable<string>


@Html.ActionLink("Home", "Index", "Home")

@foreach (var link in Model) {
    @Html.RouteLink(link, new
{
    controller = "Caliber",
    action = "List",
    caliberName = link,
    page = 1
},
new {@class = link == ViewBag.SelectedCaliber.Name ? "selected" : null })
}